<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FinalGurbaniSearcher" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="SearchViewMy" parent="Widget.AppCompat.Light.SearchView">
        <item name="submitBackground">@color/homePageOtherColor</item>
        <item name="queryBackground">@color/homePageOtherColor</item>
    </style>
    <!-- Defined a new style with three items of color. -->
    <style name="TopAppBarTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/topBarcolor</item>
        <item name="android:windowBackground">@drawable/splash_screen_grbani_searcher</item>
        <!-- Defining that new color in ColorPrimaryDark -->
        <item name="colorPrimaryDark">@color/topBarcolor</item>
        <item name="colorAccent">@color/topBarcolor</item>
    </style>

    <style name="menu_text_style" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:textSize">10sp</item>
        <item name="android:background">@color/topBarcolor</item>
        <item name="android:textColor">@color/sliver</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>